§   CONVOLUTION

+   s : ℤ → ℤ

    s = ( n : n ≥ 0 : Σ( i : 0 ≤ i ≤ n : f.i * f.(n-i) ) )

+   f : ℤ → ℤ

    f.0 = 2

    f.(k+1) = 3 * f.k, for k ≥ 0

[0] s.0 = 4

[1] s.(n+1) = (3 * s.n) + (3 * f.n * 2)

+   inv : ℤ → ℤ → ℤ → 𝔹

    inv = ( n,z,y : n ∈ [0,N] : z = s.n ∧ y = f.n )

[2] inv.0 = ( z,y :: z = 4 ∧ y = 0 )

[3] inv.(n+1) = ( z,y : 0 ≤ n+1 ≤ N
                      : z = (3 * s.n) + (3 * f.n * 2)
                      ∧ y = 3 * f.n
                )

[4] { N ≥ 0 } n,z,y:= 0,4,2 { inv.n.z.y }

[5] { inv.n.z.y ∧ n ≠ N }
      z,y:= (3 * z) + (3 * y * 2),3 * y
    { inv.(n+1).z.y }

[6] { inv.n.z.y ∧ n ≠ N }
      y:= 3 * y
    ; z:= (3 * z) + (y * 2)
    { inv.(n+1).z.y }

■   CONVOLUTION